/**
 * @file
 * Behaviors for the VARTHEME_MATERIAL_SUBTHEME theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.VARTHEME_MATERIAL_SUBTHEME = {
    attach: function (context) {
      // Vartheme Material subtheme JavaScript behaviors goes here.
      }
  };

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
